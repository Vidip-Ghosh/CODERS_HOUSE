import React from 'react'

const StepUserNameComponent = ({onNext}) => {
  return (
    <>
      <div>StepUserNameComponent</div>
      <button onClick={onNext}>Next</button>
    </>
  )
}

export default StepUserNameComponent
